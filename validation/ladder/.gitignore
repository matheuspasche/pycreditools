# Regenerated by build_base.py / run_all — not versioned (large / binary / bulky data).
shared_base_*.parquet
shared_base_*.xlsx
__pycache__/
_executed_check.ipynb
# Raw ladder data is LOCAL only. Git tracks ladder_summary.md (from build_summary.py)
# and the *_manifest.json (small: SHA-256 + take-up curve). Everything else in
# results/ is regenerable via run_all.
results/
