# Regenerated by build_base.py / run_all — not versioned (large / binary).
shared_base_*.parquet
shared_base_*.xlsx
__pycache__/
_executed_check.ipynb
# Manifests (small, hold the SHA-256 + take-up curve) and result JSONs ARE tracked.
